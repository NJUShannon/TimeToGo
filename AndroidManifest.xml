<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moonleaper.timetogo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
	<uses-permission 
	    android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> 
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.moonleaper.timetogo.TodoListManagerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.moonleaper.timetogo.AddNewTodoItemActivity"
            
            android:label="@string/title_activity_add_new_todo_item" >
        </activity>
        <activity
             android:name="com.moonleaper.timetogo.alarm.AlarmActivity">
         </activity>
        <activity
             android:name="com.moonleaper.timetogo.alarm.AlarmRing" >

             </activity>
         <service
             android:name="com.moonleaper.timetogo.WeatherAnalysis">

             </service>

    </application>

</manifest>